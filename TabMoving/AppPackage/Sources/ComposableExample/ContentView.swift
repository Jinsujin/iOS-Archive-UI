import SwiftUI

struct ContentView: View {
    let text = "Hello"
    var body: some View {
        Text(text)
    }
}

struct SwiftUIView_Previews: PreviewProvider {
    static var previews: some View {
        ContentView()
    }
}
